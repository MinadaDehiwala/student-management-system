# Student Management System (macOS Package)

This folder is the macOS-ready version of the app.

## Quick Start
From the repository root:

```bash
cd mac
chmod +x scripts/*.sh
./scripts/dev_up.sh
```

Then open:
- `http://localhost:8000/login.php`

## Full Setup Guide
- See `mac/README_MAC_SETUP.md`
